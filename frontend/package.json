{
  "name": "communication-frontend",
  "version": "1.0.0",
  "description": "Frontend pour application de communication",
  "scripts": {
    "build": "echo 'No build needed for static site'",
    "start": "npx serve -s . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}